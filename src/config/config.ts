import { environment } from '../environment/environment';

export const BASE_ENDPOINT = environment.baseUrl + '/api/';
export const TOKEN_KEY = 'cdl_token';

export const links = {
  whatsapp: 'https://chat.whatsapp.com/DmsyPbaNT375TlnccC6M2T',
};
